<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form" model="ir.ui.view">
        <field name="name">view.move.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
        <xpath expr="//button[@name='action_invoice_sent']" position="before">
            <field name="use_partner_credit_limit" invisible="1"/> 
        </xpath>
        <xpath expr="//button[@name='action_invoice_sent']" position="attributes">
            <attribute name="invisible">state != "posted" or is_being_sent or invoice_pdf_report_id or move_type not in ["out_invoice", "out_refund"] or ( payment_state not in ["paid", "in_payment"] and not use_partner_credit_limit) </attribute>
        </xpath>
        </field>
    </record>
</odoo>
